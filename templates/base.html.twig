<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Système de Facturation{% endblock %}</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Font Awesome pour les icônes -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">

    <!-- Custom CSS -->
    <style>
        body {
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        main {
            flex: 1;
        }

        .footer {
            padding: 2rem 0;
            margin-top: 2rem;
        }

        .navbar-brand img {
            height: 40px;
            margin-right: 10px;
        }

        /* Couleur du texte actif dans la navbar */
        .navbar-nav .nav-link.active {
            font-weight: bold;
            color: #0d6efd;
        }
    </style>

    {% block stylesheets %}{% endblock %}
</head>
<body>
<!-- Barre de navigation -->
<nav class="navbar navbar-expand-lg navbar-light bg-light shadow-sm">
    <div class="container">
        <a class="navbar-brand d-flex align-items-center" href="{{ path('invoice_list') }}">
            <i class="fas fa-file-invoice-dollar fs-3 text-primary me-2"></i>
            <span>Système de Facturation</span>
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbarNav" aria-controls="navbarNav"
                aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link {% if app.request.get('_route') == 'invoice_list' %}active{% endif %}"
                       href="{{ path('invoice_list') }}">
                        <i class="fas fa-list me-1"></i> Liste des factures
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {% if app.request.get('_route') == 'create_invoice' %}active{% endif %}"
                       href="{{ path('create_invoice') }}">
                        <i class="fas fa-plus me-1"></i> Nouvelle facture
                    </a>
                </li>
            </ul>

            <div class="d-flex">
                <div class="dropdown">
                    <button class="btn btn-outline-primary dropdown-toggle" type="button"
                            id="userDropdown" data-bs-toggle="dropdown" aria-expanded="false">
                        <i class="fas fa-user me-1"></i> Mon compte
                    </button>
                    <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
                        <li><a class="dropdown-item" href="#"><i class="fas fa-cog me-1"></i> Paramètres</a></li>
                        <li><hr class="dropdown-divider"></li>
                        <li><a class="dropdown-item" href="{{ path('logout') }}"><i class="fas fa-sign-out-alt me-1"></i> Déconnexion</a></li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</nav>

<!-- Contenu principal -->
<main class="py-4">
    <div class="container">
        {% block body %}{% endblock %}
    </div>
</main>

<!-- Footer -->
<footer class="footer bg-light border-top">
    <div class="container">
        <div class="row">
            <div class="col-md-4 mb-3 mb-md-0">
                <h5 class="text-primary">Système de Facturation</h5>
                <p class="text-muted">Une solution simple et efficace pour gérer vos factures.</p>
            </div>

            <div class="col-md-4 mb-3 mb-md-0">
                <h5 class="text-primary">Liens rapides</h5>
                <ul class="list-unstyled">
                    <li><a href="{{ path('invoice_list') }}" class="text-decoration-none">Liste des factures</a></li>
                    <li><a href="{{ path('create_invoice') }}" class="text-decoration-none">Créer une facture</a></li>
                    <li><a href="#" class="text-decoration-none">Aide</a></li>
                </ul>
            </div>

            <div class="col-md-4">
                <h5 class="text-primary">Contact</h5>
                <ul class="list-unstyled text-muted">
                    <li><i class="fas fa-envelope me-2"></i> contact@facturation.fr</li>
                    <li><i class="fas fa-phone me-2"></i> +33 1 23 45 67 89</li>
                    <li><i class="fas fa-map-marker-alt me-2"></i> 123 Rue de la Facturation, 75000 Paris</li>
                </ul>
            </div>
        </div>

        <hr>

        <div class="d-flex justify-content-between align-items-center">
            <p class="small text-muted mb-0">&copy; {{ "now"|date("Y") }} Système de Facturation. Tous droits réservés.</p>
            <div>
                <a href="#" class="text-muted me-2"><i class="fab fa-facebook-f"></i></a>
                <a href="#" class="text-muted me-2"><i class="fab fa-twitter"></i></a>
                <a href="#" class="text-muted me-2"><i class="fab fa-linkedin-in"></i></a>
                <a href="#" class="text-muted"><i class="fab fa-github"></i></a>
            </div>
        </div>
    </div>
</footer>

<!-- Bootstrap JS Bundle with Popper -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

{% block javascripts %}{% endblock %}
</body>
</html>